# ── Node / dependencies ─────────────────────
node_modules/
backend/node_modules/

# ── Environment files — NEVER commit real secrets ──
.env
.env.local
.env.production
.env.development.local
.env.test.local
.env.production.local
backend/.env
backend/.env.local
backend/.env.production

# ── Build outputs (upload dist/ to Hostinger manually — don't commit) ──
build/

# ── Editor / OS artefacts ───────────────────
.DS_Store
Thumbs.db
*.log
*.swp
.idea/

# ── TypeScript ──────────────────────────────
*.tsbuildinfo

# ── Security — never commit these ───────────
*.pem
*.key
*.cert
create-first-admin.js
create-first-admin.ts
seed_admin.js
backend/seed_admin.js

# ── Backend logs ─────────────────────────────
backend/logs/
